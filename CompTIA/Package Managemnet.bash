#Package Management
#dnf is the default rpm package manager that is supposed to replace yum, with its configuration file being at /etc/dnf/dnf.conf

#the /etc/yum.conf file is the primary configuration file for yum commands

#The /etc/yum.repos.d directory contains files that end in .repo and that are used to specify the location of yum repositories. Each file defines one or more repositories



#mmand to manage Debian packages that are located on a repository. This command makes use of the /etc/apt/sources.list file to determine which repository to use

#The /etc/apt/sources.list file contains a list of URLs of software repositories; there can also be files in the /etc/apt/sources.list.d directory that provide this information

#